;;--------------------------------------------------
;; File name    :   linux-00-init.el
;;              :   emacs の基本的な設定
;;              :
;;--------------------------------------------------
;;
;=======================================================================
; パスを通す
;=======================================================================
(setenv "PATH" (concat (getenv "PATH") ":~/local/bin"))
(add-to-list 'exec-path "~/local/bin")
